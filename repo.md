# mc-plugins — Task List

Nothing open. Everything on the old list has shipped: the rewrites and cleanup, the perf work,
the JUnit + MockBukkit test suite, CI + the pre-commit hook, the three flagged fixes, and the
toolchain upgrade (Paper 1.21.11 / Java 25, latest Maven plugins, JUnit 6, MockBukkit 4.110.0,
GitHub Actions checkout v7 / setup-java v5).

Build, version posture, and the module-system conventions live in `CLAUDE.md`.
